# Dashboards as files, not as clicks. Same reasoning as datasources: reproducible
# from an empty state, reviewable in a diff.
apiVersion: 1

providers:
  - name: phase-3
    type: file
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
